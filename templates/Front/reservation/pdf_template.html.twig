<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ event.NomEvent }} - Détails de l'événement</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f8f8;
            padding: 20px;
            color: #333;
            position: relative;
        }
        .event-header {
            text-align: center;
            margin-bottom: 30px;
        }
        .event-header h1 {
            color: #006666;
            font-size: 32px;
            margin-bottom: 10px;
            text-transform: uppercase;
        }
        .event-logo {
            position: absolute;
            top: 20px;
            left: 20px;
            width: 100px;
            height: auto;
        }
        .event-date {
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 18px;
            color: #888;
        }
        .event-details {
            background-color: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }
        .event-details h2 {
            color: #006666;
            font-size: 24px;
            margin-bottom: 20px;
            text-transform: uppercase;
        }
        .event-details p {
            margin-bottom: 10px;
        }
        .event-signature {
            text-align: right;
            font-style: italic;
            color: #888;
        }
    </style>
</head>
<body>
    <div class="event-header">
        <img src="{{ imagePath }}" />

        <h1>{{ event.NomEvent }}</h1>
        <div class="event-date">
            <p>Date: {{ "now"|date("d/m/Y") }}</p>
        </div>
    </div>
    <div class="event-details">
        <h2>Détails de l'événement</h2>
        <p><strong>Organisateur:</strong> {{ event.organisateur }}</p>
        <p><strong>Date Début:</strong> {{ event.Date_deb|date('d/m/Y') }}</p>
        <p><strong>Date Fin:</strong> {{ event.Date_fin|date('d/m/Y') }}</p>
        <p><strong>Capacité:</strong> {{ event.Capacite }}</p>
        <!-- Ajoutez plus de détails ici si nécessaire -->
    </div>
    <div class="event-signature">
        <p>Signature: <strong>BourLaFourme</strong></p>
    </div>
</body>
</html>
